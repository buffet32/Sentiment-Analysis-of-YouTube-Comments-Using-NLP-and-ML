python -m venv .venv
source .venv\Scripts\Activate.ps1     # or .venv\Scripts\activate on Windows
pip install --upgrade pip
pip install transformers datasets torch scikit-learn pandas tqdm pytorch-lightning
# optionally for APIs:
pip install google-api-python-client tweepy
p